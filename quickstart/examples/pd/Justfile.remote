# Justfile.remote
# Use this Justfile within the cluster.

MODEL := "Qwen/Qwen3-0.6B"
URL := "http://infra-pd-inference-gateway/v1/completions"

eval:
    lm_eval --model local-completions --tasks gsm8k \
    --model_args model={{MODEL}},base_url={{URL}},num_concurrent=10,max_retries=3,tokenized_requests=False \
    --limit 100
